#pragma once
// Generated include shim: usbipdcpp expects libusb-1.0/libusb.h, while the source
// header is libusb/libusb.h. CMake writes this under build/libusb-shim and exposes
// it through the generated pkg-config Cflags.
#include "@LIBUSB_HEADER_PATH@"
